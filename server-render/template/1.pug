doctype
html    
    head
        meta(charset='utf-8')
        meta(name='site',content='test')
        title = title   // 变量
        script alert(1);
        script.
            window.onload=function(){

            }
    body